// Create a <Button /> component that changes its color based on a color prop.
function ColorProp({color}){
    return(
<>
    <button style={{color}} >button</button>
</>
    );
    
}
export default ColorProp;